[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests that require a running OpenSearch instance
    slow: tests that ingest large PDFs or load embedding models
